---
id: "NH-0013"
title: "Build the transactional execution and proof foundation"
severity: "release-blocker"
status: "in_progress"
targetRelease: "v2.1.0"
auditIds: ""
areas: "server execution contracts, capability broker, transaction tests, execution UI"
createdAt: "2026-07-11T10:18:30.000Z"
updatedAt: "2026-07-13T09:34:00.000Z"
---

# Build the transactional execution and proof foundation

## User impact

Agent exploration currently relies on workspace-bounded tools and approvals, but it does not yet provide a transaction boundary that can predict effects, prove what occurred, or commit and roll back an isolated result atomically. This foundation must exist before parallel solution search or broader computer control can be enabled safely.

On 2026-07-13, run `3kozclNFrE-aBFAV_9Z8V` exposed a severity-one containment failure: the configured workspace was the live NexusHarness checkout, implicit compatibility mode exposed an unrestricted host PowerShell, Approval mode was disabled, and a timed-out shell left a descendant server process alive. The run overwrote harness sources and package metadata and used the operator's Git authority to commit and push. Release is blocked until self-host overlap, implicit host execution, unreviewed compatibility execution, and descendant-process leakage fail closed.

## Acceptance criteria

- Versioned schemas define objectives, contracted actions, capability leases, expected effects, observed effects, receipts, cell specifications, cell lifecycle, and commit receipts.
- Runtime validation rejects malformed contracts, expired leases, undeclared consequential capabilities, unsupported schema versions, and unsafe repository-relative scopes.
- Every contract and receipt has a deterministic canonical digest suitable for later attestation chaining.
- A provider-neutral execution-cell interface separates transaction semantics from Git worktrees, Windows isolation, Linux microVMs, or future remote providers.
- The first portable provider creates and tears down disposable repository worktrees, records the immutable base revision, detects stale-base commit attempts, and never describes worktree separation as a security boundary.
- A capability broker is the only supported entry point for contracted mutating actions and records predicted-versus-observed effects.
- Legacy compatibility mode is never selected implicitly; deliberate host execution requires both an explicit compatibility selection and a separate host-exposure acknowledgement, remains fully auditable, and cannot start unless Approval mode is enabled.
- Settings and run startup resolve real paths and reject a workspace equal to, containing, or contained by the NexusHarness installation, including link/junction aliases.
- Every new task receives an owned clean Git export at the current operating-system user's `~/.nexusharness/<task-id>` path; model tools and validation bind to that run path rather than the configured source workspace, and resume refuses to adopt an unmarked directory.
- Canceling or timing out a host command terminates its descendant process tree. One service instance may admit multiple run identities concurrently, while duplicate activation of the same run remains rejected.
- Unit and integration tests cover schema validation, digest stability, lease expiry, path boundaries, lifecycle transitions, teardown, crash recovery, stale-base rejection, and unchanged-primary-workspace behavior.
- Operator UI exposes lifecycle, provider, base revision, capability envelope, effect variance, verification state, and commit/rollback availability without presenting unfinished isolation as hardened.
- Documentation distinguishes process/worktree isolation from Windows Sandbox and Firecracker security boundaries.

## Delivery slices

1. **Contracts:** typed domain model, validators, canonical digests, lifecycle rules, and tests.
2. **Broker:** contracted-action enforcement, short-lived leases, effect receipts, compatibility adapter, and audit linkage.
3. **Portable cells:** disposable locked worktrees, base compare-and-swap, normalized diffs, teardown, and recovery.
4. **Operator surface:** execution inspector, effect comparison, verification evidence, and guarded commit/rollback controls.
5. **Hardened providers:** Windows and Linux adapters plus adversarial isolation review; these remain opt-in until separately verified.

## Dependencies and blockers

- v2.0.0 remains separately gated by HR-002 and HR-003. This v2.1 work must not be used to imply those checks passed.
- The portable worktree provider is a transaction-isolation mechanism, not a host-security sandbox.
- Hardened provider defaults require platform-specific threat modeling and a separate security review.
- Branch-and-prove search, adaptive routing enforcement, and semantic desktop control must not depend on unfinished or advisory-only contracts.

## Verification strategy

- Perspective one: deterministic model and validator tests, including mutation/fuzz cases for contracts and scopes.
- Perspective two: real temporary Git repositories proving primary-workspace immutability, cleanup, commit conflict behavior, and recovery after interrupted cells.
- Perspective three: adversarial broker tests attempting undeclared paths, commands, network destinations, expired leases, and receipt tampering.
- Perspective four: production-browser checks for truthful lifecycle and approval language once the operator surface exists.

## Progress

Severity-one containment remediation is active. The vulnerable service was stopped after confirming there were no active runs, no loaded Ollama model, and zero GPU utilization. Incident evidence and the failed generated output were preserved under the ignored Nexus data directory; the host repository and dependency graph were restored in compensating commits without rewriting the pushed incident commit.

The containment patch now fails closed without an explicit execution mode, requires a second host-exposure acknowledgement plus Approval mode for legacy compatibility, rejects Nexus installation overlap through realpath aliases, terminates descendant shell processes, and serializes service-level run admission. New tasks initialize an owned clean repository at the logged-in user's `~/.nexusharness/<task-id>` path, persist that path on the run, bind every model file/shell action to it, and show the export in run detail. A real qwen2.5-coder:14b production run created `C:\Users\dmitr\.nexusharness\u0r3p1E3JLroXTnWMuz-y\containment-smoke.txt`, passed transactional `git diff --check`, scored 8/10, promoted atomically, destroyed its worktree, left the Nexus source unchanged, replayed tool events through Glassbox SSE, and left no child Node process.

Forward-looking concurrency review removed the temporary service-wide single-run admission lock. Reservations are keyed by run ID: different tasks can plan and execute concurrently in independent export repositories and transaction cells, while duplicate activation of the same run remains rejected. A public-API E2E test launches two tasks with `Promise.all`, proves distinct run IDs, export paths, and cell IDs, then verifies both independently promoted outputs.

Delivery slice 1 is complete. The provider-neutral v1 model now covers objectives, normalized capability envelopes, time-bounded leases, contracted actions, expected and observed effects, cell specifications and live records, snapshots, effect sets, action receipts, commit receipts, finite lifecycle transitions, and the provider interface. Contract authorization binds one active lease to the same objective and cell, prevents a contract from outliving or exceeding that lease, and produces normalized SHA-256 digests from plain JSON data.

Validation rejects unsupported versions, unknown fields, expired/used/revoked leases, undeclared risk capabilities, unsafe repository paths, unsafe network origins, non-finite or non-JSON digest input, blocking variance hidden inside a successful receipt, and stale or contradictory commit receipts. The portable provider, operator UI, and hardened adapters remain unimplemented and are not advertised as available.

Evidence: 27 focused contract tests pass. The independent full release gate also passes 111 unit/component/model tests, six accessibility suites, three consequential browser workflows, 36 visual baselines, four performance suites, production smoke, package integrity, build, lint, version identity, and control verification.

Delivery slice 2 is complete. New contracted actions now have one broker entry point with explicit enforced/compatibility modes. The broker validates contract/lease linkage, requires a same-version policy decision, atomically claims single-use leases, obtains effects from an external observer rather than an operation/model response, blocks missing/forbidden/unexpected/mismatched effects, hashes canonical output, chains receipts per cell, and emits audit summaries without payload or raw errors. Policy-evaluator outages fail closed without consuming the lease; observer crashes create failed receipts without leaking error text. Existing local tools remain on their pre-contract path and are not falsely described as broker-enforced.

Evidence: 16 focused broker tests cover policy denial/outage/version mismatch, concurrent lease claims, receipt chaining, wildcard effect matching, all blocking variance classes, operation and observer failures, non-canonical output, expired/over-broad authority, explicit compatibility mode, audit redaction, and rejection of model-reported effects. The independent full release gate passes 127 unit/component/model tests plus all accessibility, workflow, visual, performance, smoke, packaging, build, lint, version, and control gates.

Delivery slice 3 is complete. The portable provider requires a clean primary repository, resolves prospective data paths before creating directories, rejects repository-overlapping links/junctions, creates locked detached worktrees outside the repository, persists validated lifecycle records atomically, inventories bounded create/update/delete/rename effects, and hashes file state without following symbolic links. Promotion requires `ready_to_commit`, at least one action-receipt digest, observable effects, a clean primary workspace, and an unchanged base; it creates a detached candidate and uses fast-forward promotion. Owned worktrees are explicitly unlocked, removed, and checked for disappearance. Interrupted preparing/executing/verifying cells recover as failed for inspection, while stale locks are reported rather than silently deleted.

The provider disables Git hooks and signing for its internal commands, rejects tampered record/candidate metadata, constrains all owned paths, fails on excessive Git/file inspection output, and retains the truthful `securityBoundary = false` classification. It does not claim to isolate hostile processes, filters, or arbitrary commands.

Evidence: 13 focused real-repository tests cover locked preparation, concurrent cell collision, dirty primary rejection, lexical and symlink/junction data-boundary rejection, complete effect inventory, contract/lease/receipt identity, observation snapshots, successful promotion and teardown, stale and dirty promotion rejection, mandatory receipt/effect evidence, interrupted recovery, record tampering, stale locks, and cross-platform-safe identifiers. The independent full release gate passes 140 unit/component/model/integration tests plus every accessibility, workflow, visual, performance, smoke, packaging, build, lint, version, and control gate.

Delivery slice 4 is complete. Run records can optionally carry a bounded execution summary, and run detail renders it through a dedicated operator inspector only when real data exists. The inspector exposes provider and security-boundary claims, finite lifecycle, full base revision, network default, capability scopes, resource ceilings, observed effects, effect variance, verification evidence, and explicit commit/rollback eligibility. Actions remain disabled when the summary or backend callback does not authorize them, with the real blocking reason available in the footer and control title. Legacy runs render exactly as before.

Evidence: three rendered component tests cover semantic structure, truthful worktree warnings, disconnected and connected actions, terminal failure, proof, effects, and variance. Five isolated production workflows verify the populated inspector, guarded promotion, a scoped axe WCAG A/AA scan, and 320 CSS px reflow without horizontal overflow. A full 1440 px populated state was rendered and visually inspected. The independent release gate passes 143 unit/component/model/integration tests, five consequential workflows, six accessibility suites, all 36 existing visual baselines, four performance suites, production smoke, package integrity, build, lint, version, and control validation.

Integration prerequisite complete: compact state polling and paged run history now project run list items without the optional execution summary, while the dedicated run-detail endpoint preserves complete capability, effect, variance, and evidence data. The projection copies rather than mutates stored runs. A seeded production smoke process proves both bounded endpoints omit execution data and the detail endpoint retains it. The full release gate passes 144 tests and all browser, performance, packaging, build, lint, version, and control gates.

Transaction lifecycle integration is complete. A provider-neutral service now serializes every operation per cell, validates prepared provider identity and spec digests, drives explicit executing/verifying/ready/committed/rolled-back/destroyed transitions, refreshes provider-observed state after failures, and publishes mutation-safe `RunExecutionSummary` projections through an injected persistence boundary. Promotion requires successful action receipts, no blocking variance, and passing evidence; commits receive canonical action-receipt digests. Rejected promotions remain truthfully retryable with their prior reason, committed changes require a new compensating transaction rather than a false rollback claim, and oversized detail projections disclose truncation while full provider records remain authoritative.

Evidence: nine transaction-service tests cover the happy lifecycle, failed proof gates, rollback versus teardown, committed compensation semantics, provider identity mismatch, concurrent serialization, receipt limits, mutation isolation, revision normalization, bounded-summary disclosure, terminal failure refresh, and full composition with the real portable provider in a temporary Git repository. The composed test proves the primary checkout is unchanged before verification, promotion occurs only after the proof gate, and provider resources tear down afterward. Existing contract and portable-provider suites, strict lint, and both TypeScript production builds also pass. Existing live tools are still on the compatibility path; migrating run creation and all tool execution remains the next integration slice.

Live migration review corrected the delivery order. Harness-enforced file reads, writes, and deletes can be redirected into a portable cell, but a Git worktree cannot enforce hostile-process or network denial for model-supplied shell commands, and it cannot roll back MCP remote effects. Full tool migration must therefore not precede hardened execution or claim that `networkDefault: deny` is an operating-system boundary. The safe sequence is run persistence and deterministic file routing, hardened providers, then shell/validation migration and explicit MCP compensation or remote-provider semantics.

The first live integration adapter is complete. `createRunTransactionService` binds every lifecycle publication to one trimmed run identity, while the durable store sink rejects unknown runs, cross-cell rebinding, malformed timestamps, and out-of-order summaries. Attached summaries are cloned before storage, and run timestamps advance without regressing. Four focused persistence tests plus the nine transaction-service tests pass alongside strict lint and both production TypeScript builds. Deterministic file dispatch remains the next sub-slice; no existing shell or MCP behavior is falsely labeled transactional.

Approval-safe transaction admission is complete. Providers can optionally preflight an action inside the isolated cell context before the transaction service enters `executing`. The portable provider validates cell, contract, and lease identity under its cell lock and exposes the owned worktree only to the trusted action-admission adapter. If approval or another prerequisite stops admission, the cell remains `isolated` or `verifying`, the action executor is never called, no receipt is consumed, and the operation can be retried after operator input. Focused verification passes 51 transaction-service, portable-provider, and contract tests plus strict lint and both production builds. The remaining file-action adapter must use this hook rather than requesting approval after execution begins.

The deterministic portable file-write executor is complete. Approval and mutation are split by a digest-bound plan containing the relative path, before/after hashes and sizes, bounded diff, and in-memory content. Contract admission rejects unregistered actions, payload mismatch, authority mismatch, and missing predicted create/update effects before operator approval. Execution re-resolves the cell path and refuses time-of-check/time-of-use drift, then runs through the enforced capability broker and records the actual create/update digest without placing raw content in broker audit metadata. A post-operation state check still reports an applied effect if a later audit write fails, keeping receipts aligned with filesystem truth. Four dedicated adversarial tests plus transaction-service and real portable-provider suites pass (28 focused tests total), along with strict lint and both production builds. Agent-loop dispatch is not connected yet, and no shell or MCP action is represented by this file-only executor.

Windows hardened-provider work has started with a launcher boundary, not a falsely complete provider. The candidate verifies platform and launcher presence, writes an exclusive temporary `.wsb` profile outside the mapped cell, maps exactly one non-root cell folder, disables vGPU, network, audio/video input, printers, and clipboard, enables Protected Client, bounds memory and runtime, launches with a reduced environment and hidden host process window, honors aborts, rejects overlapping configuration paths, and removes the profile on success or failure. Its `securityBoundary` remains `false` until the real-host checkpoint passes. Eleven focused tests cover hardened XML, escaping, unsafe bootstrap names, root/resource rejection, probe truthfulness, runner invocation, cleanup, and mapped/config path separation; lint, builds, and version identity pass.

The Windows host exposes `WindowsSandbox.exe` and DISM reports `Containers-DisposableClientVM` enabled. `npm run test:windows-sandbox` now creates a disposable owner-run probe that checks seed visibility, mapped writeback, Sandbox guest identity, disabled TCP egress, automatic shutdown, host result visibility, and cleanup. HR-004 records the required interactive evidence. This launcher still lacks execution-cell record persistence, brokered command transport, effect harvesting, and atomic promotion; those remain required before it implements `ExecutionCellProvider` or becomes selectable by live runs.

The first HR-004 attempt exposed a real asynchronous-launch defect rather than a missing host feature. `WindowsSandbox.exe` handed the configuration to the Sandbox service and exited successfully before the guest initialized; the launcher treated that short-lived process exit as completion, deleted the `.wsb` profile, and the probe removed its mapped folder. Windows Sandbox then reported `0x80070003` and the host could not find `result.json`. Launch completion is now bound to one validated guest-written completion filename. The launcher removes any stale sentinel before start, retains configuration and mapped data after host process exit, polls abortably within the original total deadline, and only cleans the profile after the sentinel appears or the launch fails. Four new regression/validation cases bring the focused launcher suite to 15 passing tests; lint and both builds pass. HR-004 remains open for a retry after this repair is pushed.

The second HR-004 attempt advanced through guest execution and exposed shutdown ownership. The bootstrap wrote its result and then invoked forced guest shutdown, which the Windows Sandbox client surfaced as connection loss `0x80072746`; host cleanup then encountered `EBUSY` while the mapped cell remained mounted. The probe now snapshots pre-existing `WindowsSandboxRemoteSession` process IDs, never requests guest shutdown, closes only session IDs created by the probe, preserves unrelated user sessions, and retries Windows transient directory locks for a bounded 15 seconds. Both diagnostic captures are retained locally but excluded from npm packages. The 15 focused launcher tests, lint, builds, and package manifest checks pass; HR-004 remains open for a third real-host attempt after push.

The third HR-004 attempt successfully returned all four guest boundary assertions as true under `WDAGUtilityAccount`: seed visibility, mapped write, disabled TCP egress, and Sandbox identity. Host decoding alone failed because Windows PowerShell 5.1 `Set-Content -Encoding UTF8` emitted a byte-order mark that Node's strict `JSON.parse` rejected. The bootstrap now writes JSON with `System.Text.UTF8Encoding(false)`, and the shared parser strips a leading BOM defensively for compatibility with already-produced output. A dedicated plain/BOM regression brings the focused launcher suite to 16 passing tests; lint, builds, and package verification pass. HR-004 remains open for one confirmation run that can also evaluate host writeback and print the final `passed` field.

The fourth HR-004 attempt stopped safely before launching a VM because the preflight `Get-Process` query returned exit code 1 for the normal state in which no `WindowsSandboxRemoteSession` existed. Session discovery now wraps results in an explicit PowerShell array, emits IDs only when present, and terminates with explicit success. Parsing is shared and accepts only positive safe-integer PIDs. A focused empty/multiple/invalid session test brings the launcher suite to 17 passing tests, and the exact child-PowerShell query was separately executed on this host with `queryExit=0`; lint and both builds pass. No Sandbox boundary behavior regressed in this attempt.

HR-004 passed on the real Windows host. The final owner run reported `seedRead`, `mappedWrite`, `networkBlocked`, `sandboxIdentity`, `hostWriteback`, and aggregate `passed` as true under `WDAGUtilityAccount`, then exited cleanly with `Windows Sandbox isolation probe passed.` The launcher can now truthfully report `securityBoundary = true` for its verified Windows Sandbox virtualization profile. This acceptance applies to the hardened launcher boundary only: execution-cell state persistence, brokered command/result transport, effect harvesting, atomic promotion, crash recovery, and live-run selection remain unfinished and cannot inherit completion from HR-004.

The Windows execution-cell provider composition is complete. `WindowsSandboxProvider` accepts only action executors carrying the explicit `windows-sandbox` isolation identity and maps an external `windows-sandbox` cell specification onto the proven disposable-worktree transaction engine without leaking the inner portable provider identity. Prepared cells, transitions, snapshots, and recovered records expose the original Windows spec digest and provider reference. Effects remain in the detached worktree until receipt-gated fast-forward promotion; teardown and interrupted-state recovery reuse the hardened transaction invariants. The underlying provider now exposes only cloned spec/cell inspection records for safe composition.

Three Windows-provider integration cases use real temporary Git repositories to prove unchanged-primary execution, mapped provider identity, effect inventory, verified promotion, owned-worktree teardown, interrupted recovery, non-Windows spec rejection, and rejection of ordinary host executors. A separate portable-provider test proves inspection records cannot mutate persisted state. Thirty-five focused Windows/portable tests, strict lint, and both production builds pass. The concrete Windows Sandbox command/result executor is still required before model or validation shell commands can use this provider; a structural isolation marker prevents accidental host execution but is not treated as a defense against malicious in-process dependency injection.

Windows remote-session ownership is now part of `WindowsSandboxLauncher` rather than special-case probe code. The launcher snapshots existing `WindowsSandboxRemoteSession` IDs before writing/starting the profile, discovers sessions again on every exit path, and stops only newly created positive safe-integer IDs. Pre-existing user sessions are preserved. Session-list failures during teardown cannot prevent profile cleanup, and a session-stop failure still reaches the nested configuration cleanup. The probe now delegates session lifecycle entirely to the launcher and retains only bounded retry for its temporary mapped directory. Twenty focused Windows launcher/provider tests, lint, and both builds pass. Brokered command/result transport remains the next slice.

The Windows Sandbox command transport candidate is complete. Commands are registered outside the model-visible contract and bound to `shell.exec`, `powershell.exe`, cell, lease, and raw payload by canonical digest. Admission rejects tampering before approval. The guest bootstrap carries only PowerShell `EncodedCommand`, captures stdout/stderr to temporary mapped files, removes those files before writing the completion sentinel, emits bounded BOM-safe JSON, and leaves no transport artifacts in the observed effect set. Successful results are available once through `takeResult`; receipts retain only output/error digests. Host-side Git observation compares pre/post changed-file state after cleanup and feeds the enforced broker, which retains its single-use lease and chained receipt guarantees.

Three command-transport adversarial tests cover successful encoded transport and observed file creation, pre-approval payload tampering, failed guest exit redaction, one-use result retrieval, and absence of the raw command in bootstrap and broker audit data. Together with the Windows provider suite, 23 focused tests, lint, and both builds pass. These tests use a controlled launcher: a real Sandbox command smoke and explicit toolchain availability policy are still required before live validation commands select this executor. The current bootstrap supports Windows PowerShell available in the base Sandbox image; it does not imply Node, Git, package managers, or project-specific binaries exist in the guest.

The real Windows command-provider probe is ready under HR-005. It builds a disposable Git repository, prepares a `windows-sandbox` cell, registers and admits a digest-bound PowerShell write, launches the real verified guest, checks the enforced receipt and one-use result, proves the primary checkout is unchanged, validates the host-observed create effect, promotes by canonical receipt digest, verifies the committed writeback, tears down the cell, and removes all temporary data. Passing this probe authorizes the base PowerShell transport only; project validation toolchains remain separately capability-gated.

The hardened Linux profile foundation is implemented but deliberately remains unverified. It produces a networkless, SMT-disabled, bounded Firecracker configuration and a jailer-only invocation plan with non-root UID/GID, explicit cgroup parent/version, PID namespace, file-size/open-file limits, and `--no-api` configuration boot. Validation is host-neutral and rejects non-normalized or traversing Linux paths, root identities, unsafe IDs/cgroups, excessive resources, and kernel/rootfs/executable/configuration collisions inside the jail. Availability requires Linux, a readable/writable character-device `/dev/kvm`, and executable Firecracker and jailer inputs. `securityBoundary` remains false.

Evidence: 16 focused tests cover configuration, jailer invocation, host-path neutrality, boundary truthfulness, traversal/collision rejection, identity/resource bounds, and non-Linux fail-closed probing. Two complete release-matrix runs passed; the final run includes 203 unit/model/integration tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification. This slice does not stage immutable kernel/rootfs/configuration inputs, create the chroot tree, launch or supervise a microVM, verify matching binary versions/seccomp/cgroups, transport commands, harvest effects, or promote results. Those require the HR-006 Linux/KVM host and later provider composition.

The live-dispatch prerequisite now supports multiple enforced portable action adapters without weakening admission. `CompositePortableActionExecutor` rejects missing, empty, or duplicate routes; binds the selected executor to contract, lease, immutable cell identity, and working directory; permits only identical idempotent re-admission after a provider-transition retry; consumes admission once; and rejects contract, authority, route, cell, or workspace substitution. Delegate-side mutation during admission also fails closed. This is infrastructure for file-delete, shell/validation, and remote-effect adapters; it does not yet change the live agent loop or make unsupported actions transactional.

Evidence: nine adversarial cases cover route selection, missing/duplicate routes, admission requirement, one-use replay, identical retry, contract/lease/kind/cell/workspace tampering, and a mutating delegate. A tenth real-repository integration composes the dispatcher with the deterministic file-write executor, portable provider, and transaction service, proving the primary checkout remains unchanged before verification, receipt-gated promotion succeeds, and teardown completes. The complete release matrix passes 213 tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification.

Deterministic regular-file deletion is now broker-enforced. Registration binds the repository-relative target, SHA-256 pre-image, and byte count before a contract exists; admission rechecks that state before contextual approval; execution rechecks it again immediately before a non-recursive unlink. Only regular files up to the portable provider's 20 MiB inspection ceiling are eligible. Contracts must grant the exact delete path and predict one required deletion without an impossible after-state digest. The observer reports deletion from filesystem truth even when a later local audit write fails. Directories and recursive deletion remain on the explicit legacy path.

Evidence: seven focused cases cover successful approval and deletion, pre-approval drift, post-approval drift, approval interruption without authority consumption, post-delete audit failure, unsafe paths, directories, duplicate registration, payload/effect mismatch, bounded large-file rejection, and real portable-provider promotion. The real Git integration proves the primary file remains present until verification and receipt-gated commit, then is deleted only by promotion and the cell tears down. The complete release matrix passes 220 tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification. Before composite dispatch is wired live, concrete adapters must also share cell-scoped lease-use and receipt-chain stores; their current separate in-memory stores are safe only while routes are not jointly selectable.

The cross-adapter authority gap is closed at the adapter boundary. Portable write and delete executors now accept injected `LeaseUseStore` and `ReceiptChainStore` instances while retaining private defaults for standalone compatibility. A run coordinator can therefore give every routed adapter in one execution domain the same atomic lease-use set and receipt-chain head. Cross-kind replay of one single-use lease is blocked before the second operation, and successful actions with distinct leases link into one cell receipt chain. Live composition must inject the shared pair; constructing routed adapters with their private defaults remains intentionally insufficient for selection.

Evidence: 23 focused composite/write/delete tests pass. New real-filesystem cases prove a write receipt is the predecessor of a later delete receipt across different adapters and prove a lease consumed by file write cannot be replayed through file delete; the target remains present and the second receipt is blocked. The complete release matrix passes 222 tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification.

The first HR-005 owner run reached the post-execution aggregate assertion and failed, but the probe printed no intermediate receipt/result/isolation/effect state before teardown. Local reproduction confirms Windows PowerShell 5.1 returns an integer exit code through the exact nested `Start-Process -Wait -PassThru` pattern, so no guest defect is being guessed from insufficient evidence. The probe now always prints a redacted execution assertion report before deciding: receipt success, result availability, zero exit, unchanged primary checkout, expected effect observation, bounded effect labels, variance classifications, and evidence statuses. It never includes command text, stdout, stderr, raw errors, digests, or temporary paths. Promotion has a separate report. HR-005 remains pending one diagnostic retry.

Evidence: 23 focused Windows command/provider tests pass, including positive and failed-report shape, raw-error exclusion, command-content exclusion, one-use result behavior, transport cleanup, provider identity, lifecycle, effect observation, and promotion composition. The complete release matrix passes 222 tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification.

The second HR-005 owner run established the failure boundary: contract policy passed and the primary checkout remained unchanged, but the operation returned no result, observed no effect, and produced a missing-effect variance. The guest transport is now fail-observable by stage. Host diagnostics distinguish bootstrap write, Sandbox launch, result read/parse/validation, guest bootstrap, guest exit, and completion, with only sanitized error type and guest stage. The guest bootstrap always writes structured `transportStatus`, `transportStage`, and error type on its own failures. It now invokes encoded child PowerShell synchronously with the call operator and captures `$LASTEXITCODE`, removing the extra `Start-Process` process-object handoff while preserving child isolation from `exit`. HR-005 remains pending a third owner run.

Evidence: the exact synchronous encoded-command handoff passes under host Windows PowerShell 5.1 with integer exit 0, captured stdout, and empty stderr. Twenty-four focused Windows command/provider tests cover complete, guest-exit, and guest-bootstrap diagnostics; stage sanitization; raw content exclusion; transport structure; cleanup; lifecycle; effects; and promotion composition. The first complete release attempt passed all 223 core tests and builds but two unchanged accessibility cases timed out under load; an isolated six-case accessibility rerun passed, followed by a second complete release matrix passing 223 tests, all accessibility/workflow/visual/performance suites, production smoke, package integrity, strict lint, both builds, version identity, and control verification.

The third HR-005 owner run reported `stage: sandbox-launch`, proving failure occurred before result read or guest execution. Static comparison then found the exact contract mismatch: `WindowsSandboxCommandExecutor` generated dot-prefixed `.nexus-…` bootstrap and completion filenames, while `WindowsSandboxLauncher` intentionally accepts only alphanumeric-first safe filenames. Controlled executor tests had not applied the launcher's naming rule. Transport artifacts now use `nexus-<token>.bootstrap.ps1` and `nexus-<token>.result.json`, and the controlled launcher fixture enforces both real filename patterns before simulating execution. No security validation was loosened. HR-005 remains pending a fourth owner run.

Evidence: 24 focused Windows command/provider tests pass with the new launcher-contract regression. The complete release matrix passes 223 tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification.

HR-005 passed on the fourth real-host run. The owner report shows `receipt: succeeded`, exit 0, unchanged primary checkout, the expected `file.create` effect for `sandbox-command-output.txt`, no variances, passing policy evidence, `diagnostic.stage: complete`, every execution check true, committed promotion, successful audit linkage, and aggregate `passed: true`. This accepts the base Windows PowerShell command executor for live validation selection behind the verified Sandbox provider. It does not imply that Node, Git, package managers, project dependencies, arbitrary shells, or any other guest toolchain is present; those remain explicit capability checks.

Portable validation commands are now broker-enforced for the run-coordinator path. The executor accepts at most 100 exact trusted allowlist entries, binds command and configured shell into the payload digest, requires that shell as execute authority, rejects network/secret capabilities and predicted repository mutations, honors contextual approval and aborts, bounds command/output/runtime through the local shell primitive, shares lease and receipt stores with other routed adapters, and releases results once. Git-visible state is observed before and after; any new mutation becomes blocking variance and a failed receipt. Existing intended cell changes are not reported again. Successful and failed raw output remains outside receipts.

This adapter has `securityBoundary = false`: it runs an operator-configured command on the host inside a disposable Git worktree. It provides transactional effect isolation, not hostile-process containment or operating-system network denial. Model-originated arbitrary shell must not select it; hardened Windows or Linux providers remain required for that boundary.

Evidence: seven dedicated cases cover success, approval, allowlist/authority mismatch, command failure redaction, undeclared mutation, result consumption, limits, boundary truthfulness, cross-adapter chaining, and a real portable-provider transaction. The real integration proves primary-checkout immutability through write and validation, receipt chaining, verification readiness, receipt-gated promotion, and teardown. Together with transaction and portable-provider suites, 32 focused tests pass. The complete release matrix passes 230 tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification.

The run-scoped portable coordinator is complete as an integration component. A stable run-derived cell owns reads/listing, deterministic writes, regular-file deletes, allowlisted validation, shared leases/receipt chain, persisted summaries, proof, fast-forward promotion, rollback, and teardown. Each action receives an exact short-lived single-use authority and predicted effect contract inside the cell's broader ceiling. Reads resolve only through the provider-validated owned worktree. Every required configured validation command must pass after the latest mutation; mutations clear prior validation evidence, validation side effects block, and `ready_to_commit` forbids later execution. Commit performs verification when needed and remains receipt-gated. Initial persistence failure after provider preparation destroys the managed worktree; pre-ownership preparation failures remain retryable.

The coordinator retains `securityBoundary = false`: it is a transactional Git/worktree boundary and allowlisted host validation, not hostile process or network isolation. It is not connected to `agentLoop` yet. Live selection must remain explicit and must not route arbitrary model shell or MCP actions into this portable path.

Evidence: eight real-repository coordinator cases cover full write/read/list/validate/verify/commit/destroy flow, persisted single-cell identity, mandatory validation, every-configured-command proof, stale-validation invalidation, validation mutation blocking, regular-file deletion, rollback, approval retry, pre-prepare rejection, terminal teardown, and persistence-failure cleanup. The complete release matrix passes 238 tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification.

The live agent loop now has an explicit execution-mode boundary. Compatibility remains the default. `NEXUSHARNESS_EXECUTION_MODE=transactional` requires an absolute `NEXUSHARNESS_EXECUTION_DIR`; the harness derives a workspace-specific external data root, prepares one stable cell after planning, serializes executor subtasks, exposes only coordinator-backed file list/read/write/regular-file-delete tools, and runs only the operator-configured lint/test allowlist through brokered validation. Successful effects are verified and promoted after critic acceptance; no-op runs roll back; failures, cancellation, and approval rejection roll back and destroy; approval-required pauses retain the live cell for same-process resume. Progress writes merge only the owning run and optional retrospective into current store state so tool-created approvals, audit records, and execution summaries are not lost to a stale whole-store snapshot.

The boundary remains truthful: portable transaction mode reports `securityBoundary=false`, does not expose arbitrary model shell or MCP, and fails closed when non-destroyed persisted state exists without its in-memory coordinator. Restart recovery, hardened-provider selection for model-originated commands, and explicit remote-effect compensation remain subsequent slices.

Evidence: five execution-mode tests cover configuration fail-closed behavior, tool-surface selection, all coordinator-routed file operations, exclusion of shell/MCP/unknown tools, and failed-receipt handling. Together with eight real-repository coordinator cases, 35 focused tests pass; server TypeScript and scoped ESLint checks pass. The independent complete release rerun passes 243 core tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification. One initial concurrent control-plane case transiently failed under the full-suite load; two simultaneous isolated reruns passed before the clean complete-matrix rerun also passed it.

Persisted portable transactions now recover safely after a process restart. Recovery is scoped to the exact cell and stable run objective, rejects cross-objective cleanup, converts interrupted preparing/executing/verifying cells to failed through the provider, removes the owned worktree, and persists a truthful destroyed summary with explicit warning evidence that in-memory authority and receipt proof were not restored. The resumed run retains its plan but clears stale executor, validation, critic, result, error, and iteration state before creating a distinct attempt cell. Store rebinding is permitted only after the preceding cell is destroyed. A transaction already recorded as committed is cleaned up but permanently blocked from automatic replay on that run to prevent duplicate effects.

Evidence: 30 focused tests pass across provider recovery, real-repository coordinator restart/discard/replacement/promotion, store attempt rebinding, and live execution-mode selection. The real Git case proves the interrupted file never reaches the primary checkout, the old worktree is removed, the replacement cell has a distinct identity under the same objective, and only the newly validated replacement effect is promoted. Server TypeScript and scoped ESLint pass. The independent complete release matrix passes 245 core tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification.

The pre-summary hard-kill window is now recoverable rather than collision-only. Portable provider recovery can enumerate validated records for one exact objective without selecting records by model-controlled filenames or touching a different objective. The coordinator captures any remaining orphan effects, discards each non-destroyed cell, and returns bounded recovery facts. Live attempts use randomized cell identities beneath the stable run objective; orphan summaries persist destroyed state and explicit warning evidence, while a committed orphan permanently blocks automatic replay. Corrupt or stale-lock records still fail closed instead of being silently skipped.

Evidence: 31 focused tests pass. New real-repository coverage simulates an unbound verifying cell with an unpublished file effect, proves objective-only discovery excludes another objective, observes the orphan effect before cleanup, removes the owned worktree, leaves the primary checkout unchanged, and prepares a fresh attempt without collision. Server TypeScript and scoped ESLint pass. The independent complete release matrix passes 246 core tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification.

The Windows Sandbox run coordinator is complete as a selectable component, but is not connected to the agent loop yet. It composes exact brokered file write/delete adapters with predicted-effect PowerShell command execution through one `WindowsSandboxActionExecutor` router and one Windows execution cell. File actions run deterministically against the owned worktree; only model-originated commands cross the HR-004/HR-005-verified Windows Sandbox boundary. The provider boundary description now makes that split explicit. All routed action kinds share one atomic lease-use set and one receipt-chain head.

Sandbox commands require the model/harness to declare every expected file create/update/delete effect in advance. Host-observed unexpected, missing, forbidden, or mismatched effects fail the receipt. Command output and sanitized diagnostics stay outside receipts; abort signals reach the guest launcher. Trusted configured validation commands predict no mutation, every configured command must pass after the latest action, and any later file or command action clears earlier validation evidence. Promotion remains verification- and receipt-gated.

Evidence: 26 focused Windows provider, command-transport, and coordinator tests pass. A real temporary Git repository with a controlled Sandbox launcher proves deterministic file write, predicted guest file creation, validation, cross-adapter receipt chaining, unchanged-primary isolation, atomic promotion, and teardown. An adversarial command that creates an undeclared file receives an unexpected-effect failed receipt and is rolled back. Server TypeScript and scoped ESLint pass. The independent complete release matrix passes 248 core tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification. Live mode selection remains the next slice.

Windows Sandbox is now an explicit live execution mode. `NEXUSHARNESS_EXECUTION_MODE=windows-sandbox` requires the same absolute external `NEXUSHARNESS_EXECUTION_DIR` as portable transactions, probes the Windows launcher before cell preparation, stores Windows cells and launcher configurations in provider-separated directories, and uses the Windows coordinator for planning-to-promotion lifecycle, approval pauses, restart recovery, orphan cleanup, validation, commit, rollback, cancellation, and rejection cleanup. Active same-process approval resumes retain their Windows tool surface even if environment configuration changes.

The executor receives transactional file list/read/write/delete plus `sandbox_exec`. Each Sandbox call requires a PowerShell command and an array of zero to 200 predicted file create/update/delete effects with repository-relative targets and descriptions. No expected effects means the command must be read-only from Git's perspective. Receipt failure is returned as a tool error even if the guest process exited zero. Portable mode still exposes no model shell; neither transactional mode exposes MCP; compatibility remains the unchanged default. Windows mode never falls back to a host command when the launcher or guest toolchain is unavailable.

Evidence: 18 focused execution-mode, portable-coordinator, and Windows-coordinator tests pass. Configuration tests cover the new explicit mode and storage requirements; tool-surface tests prove `sandbox_exec` appears only in Windows mode; dispatch tests cover predicted-effect parsing, Windows-only routing, output/diagnostic return, and failed proof. Server TypeScript and scoped ESLint pass. The independent complete release matrix passes 249 core tests, six accessibility checks, five consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification.

Transactional MCP access is restored without expanding the containment response into a new remote-effect framework. Every MCP server and tool already enabled by the operator is included in compatibility, portable-transaction, and Windows Sandbox model tool surfaces. Dispatch rechecks both enablement levels, MCP successes and failures retain network-risk audit evidence, and executor instructions state that remote MCP effects are outside file rollback and must never be used to reach NexusHarness source or bypass the run-owned export. Arbitrary host shell remains unavailable in portable transactions; isolated file routing and source/export separation are unchanged.

Evidence: focused execution-mode coverage proves enabled/disabled MCP selection, exact sanitized tool resolution, real invocation routing, external-effect disclosure, and continued host-shell exclusion. A real stdio MCP integration discovers and calls an enabled tool through the production SDK client. The complete release matrix passes 323 core tests, six accessibility checks, six consequential workflows, six visual suites, four performance suites, production smoke, package integrity, strict lint, both production builds, version identity, and control verification.
